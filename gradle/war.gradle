apply plugin: "war"

bootWar {
    mainClassName = "org.regitiny.catiny.messenger.CatinyMessengerApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
